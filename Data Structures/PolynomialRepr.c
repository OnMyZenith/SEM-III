#include <stdio.h>

struct Polynomial {
    float coef;
    int exp;
};
typedef struct Polynomial poly;

void addPoly(poly a[], poly b[], poly c[], int size1, int size2, int *size3) {
    //
}

void multPoly(poly a[], poly b[], poly c[], int size1, int size2, int *size3) {
    //
}

void multTerm(poly a[], poly term, poly b[], int size1) {
    //
}

int main() {
    poly a[10], b[10];
    int size1, size2;
    return 0;
}